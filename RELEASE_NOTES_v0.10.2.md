## SCutils v0.10.1

### 🛠️ Bug fixes and improvements.

- `FilterVariableFeatures()` now does not filter out HLA genes by default. If you still want to filter out HLA genes, you can use the `pattern` argument to specify a custom pattern for filtering.